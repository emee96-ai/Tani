<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/adminDrawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/tani_background"
        android:orientation="vertical">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/adminToolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/tani_surface"
            android:elevation="0dp"
            android:paddingStart="4dp"
            android:paddingEnd="4dp"
            app:menu="@menu/admin_toolbar"
            app:navigationIcon="@drawable/ic_admin_menu"
            app:subtitleTextColor="@color/text_muted"
            app:titleTextColor="@color/text_dark" />

        <ProgressBar
            android:id="@+id/dashboardProgress"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="3dp"
            android:indeterminate="true"
            android:visibility="gone" />

        <TextView
            android:id="@+id/dashboardStatus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/error_soft"
            android:padding="12dp"
            android:textColor="@color/error"
            android:textSize="13sp"
            android:visibility="gone" />

        <androidx.core.widget.NestedScrollView
            android:id="@+id/adminScreenScroll"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:fillViewport="true"
            android:overScrollMode="never">

            <LinearLayout
                android:id="@+id/adminScreenContainer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingStart="16dp"
                android:paddingTop="16dp"
                android:paddingEnd="16dp"
                android:paddingBottom="24dp" />
        </androidx.core.widget.NestedScrollView>

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/adminBottomNavigation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/tani_surface"
            android:elevation="10dp"
            app:itemIconTint="@color/admin_nav_colors"
            app:itemTextColor="@color/admin_nav_colors"
            app:labelVisibilityMode="labeled"
            app:menu="@menu/bottom_admin_nav" />
    </LinearLayout>

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/adminNavigationView"
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/tani_surface"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/header_admin_drawer"
        app:itemIconTint="@color/admin_nav_colors"
        app:itemTextColor="@color/text_dark"
        app:menu="@menu/drawer_admin_nav" />

</androidx.drawerlayout.widget.DrawerLayout>
