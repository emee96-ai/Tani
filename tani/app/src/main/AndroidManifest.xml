<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:usesCleartextTraffic="false"
        android:label="تاني"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tani">

        <provider
            android:name=".ui.common.NotificationBadgeInitProvider"
            android:authorities="${applicationId}.notification-badge-init"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Tani.Starting"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Production-ready Verified App Link path. Set TANI_APP_LINK_HOST
                 and TANI_RESET_REDIRECT to the real HTTPS domain in release CI. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="${taniAppLinkHost}"
                    android:path="/auth/reset" />
            </intent-filter>
        </activity>
    </application>
</manifest>